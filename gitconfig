[user]
	name = Julien Grimault
	email = julien_grimault@apple.com
[alias]
	co = checkout
  lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
  pr = pull --rebase
  pfr = send-email --quiet --to maps-patches@patches.apple.com
[color]
	diff = auto
	status = auto
	branch = auto
	ui = true
[core]
	excludesfile = ~/.gitignore
	editor = vim
	autocrlf = input
	quotepath = false
	trustctime = false
[apply]
	whitespace = nowarn
[format]
	pretty = %C(yellow)%h%Creset %s %C(red)(%an, %cr)%Creset

[diff "objcpp"]
  xfuncname = ^[-+@a-zA-Z_].*$
[diff "objcppheader"]
  xfuncname = ^[@a-zA-Z_].*$
[diff "localizablestrings"]
  textconv = "iconv -f utf-16 -t utf-8"
[diff]
	tool = Kaleidoscope
	algorithm = patience
[difftool "Kaleidoscope"]
	cmd = ksdiff --partial-changeset --relative-path \"$MERGED\" -- \"$LOCAL\" \"$REMOTE\"
[difftool]
	prompt = false
[merge]
	tool = Kaleidoscope
[mergetool "Kaleidoscope"]
	cmd = ksdiff --merge --output \"$MERGED\" --base \"$BASE\" -- \"$LOCAL\" --snapshot \"$REMOTE\" --snapshot
	trustExitCode = true
[mergetool]
	prompt = false
	keepBackup = true
[push]
	default = matching

[sendemail]
  smtpserver = relay.apple.com
  chainreplyto = true
  thread = true
  multiedit = false
  suppresscc = all
  assume8bitEncoding = UTF8
  from = Julien Grimault <julien_grimault@apple.com>
